# Force bazel output to use colors (good for jenkins) and print useful errors
common --color=yes

# Needed for building and testing
build --apple_platform_type=ios

# Simulator can not be selected by the testrunner with the default options, but they are needed for 'bazel run'
# See: https://github.com/bazelbuild/rules_apple/pull/246#issuecomment-438757348
build --ios_simulator_device="iPhone 8" 

# I do not think this is needed for now
# build --ios_simulator_version="12.4"  

# Build the app FAT, so it can be run on devices. This requires a provisioning profile
# build --ios_multi_cpus=armv7,arm64

# I do not think this is needed
# build --xcode_version=10.3

# During development
# build --test_output=all --cache_test_results=no

# References:
# - https://github.com/tensorflow/tensorflow/blob/master/tensorflow/tools/ci_build/install/.bazelrc